#include <bits/stdc++.h>
#define pub push_back
#define pob pop_back
typedef long long ll;
typedef long double ld;
using namespace std;

#define FIN(dir) freopen(dir, "r", stdin)
#define FOUT(dir) freopen(dir, "w", stdout)



int32_t main() {
	
	return 0;
}
